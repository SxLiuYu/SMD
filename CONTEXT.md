# Charlie 语音助手 — 领域模型

> 最后更新: 2026-08-14
> 维护者: sxliuyu

## Glossary

### 核心概念

| 术语 | 定义 |
|------|------|
| **Charlie** | 语音助手系统本身，运行在 Mac/PC/树莓派上 |
| **ESP32 终端** | 物理设备（LC-S3 WiFi 1.54寸 TFT），负责拾音和显示 |
| **大脑 (Brain)** | 意图识别 → LLM → MCP 工具调用的核心处理链 |
| **快路径 (FastPath)** | 无需 LLM 的规则匹配，用于简单指令（时间、场景） |
| **MCP 技能** | 通过 Model Context Protocol 暴露的工具，按 Key 分层启用 |
| **场景协议** | 一句话触发的多设备联动组合（晚安、早安等） |
| **Demo 模式** | 零配置可用模式，仅支持快路径能力 |

### 用户状态

| 术语 | 定义 |
|------|------|
| **用户状态** | 由位置、语音活动、屏幕活动推断的上下文（home_awake / home_sleeping / working / away） |
| **home_location** | 用户设定的 home 地理围栏坐标 |
| **presence_score** | 基于设备存在的置信度（0.0-1.0） |

### 会话与历史

| 术语 | 定义 |
|------|------|
| **Session** | 一次对话会话，有独立的对话历史 |
| **MAX_HISTORY** | 单会话保留的最大消息对数（20） |
| **MAX_SESSIONS** | 最大并发会话数（10） |
| **会话压缩** | 当 tokens 接近限制时，用 LLM 摘要旧对话 |

### 意图识别

| 术语 | 定义 |
|------|------|
| **唤醒词** | 设备端触发的关键词（小智/查理等），需在进入大脑前剥离 |
| **低意图 (Low Intent)** | 无明确指令的噪声输入（嗯、啊、模糊音节） |
| **乱码 (Garbled)** | ASR 识别错误产生的无意义文本 |
| **意图分类** | 将清洗后的文本分类为时间/天气/场景/控制/对话等 |

### 技能层级

| 术语 | 定义 |
|------|------|
| **核心技能 (8个)** | 时间、天气、提醒、备忘、系统、场景、摘要、文件 |
| **可选技能 (11个)** | 飞书、抖音、淘宝、音乐、做菜、衣橱、浏览器、进化、搜索、空调、笔记 |
| **Profile** | 按用户配置的 Key 组合选择启用的技能子集 |

### 硬件控制

| 术语 | 定义 |
|------|------|
| **涂鸦 IoT** | 通过涂鸦红外网关控制现有家电（空调、电视） |
| **xiaozhi 协议** | ESP32 与 Charlie 通信的自定义协议（MQTT + UDP/WebSocket） |
| **OTA** | 远程固件更新 |

## 关键关系

```
用户语音 → [ESP32] → ASR → 唤醒词剥离 → 意图分类
                                      ↓
                           快路径命中？──是──→ 直接返回
                                      ↓ 否
                                   LLM 大脑
                                      ↓
                           MCP 工具调用 → 设备控制 / 信息返回
                                      ↓
                                   TTS → ESP32 播报
```

## 设计原则

1. **零配置优先** — Demo 模式开箱即用，不填 Key 也能跑基础功能
2. **分级降级** — LLM 不可用时自动回退到规则模式
3. **物理隔离** — ESP32 固件不含敏感凭证，配网通过 AP 热点完成
4. **技能即工具** — 每个 MCP server 是独立 Python 模块，可单独启用/禁用
